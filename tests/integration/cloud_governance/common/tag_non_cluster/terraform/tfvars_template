input_variables = {
    "instance_details" = {
        "image_name" = "{{ image_name }}"
        "instance_type" = "{{ instance_type }}"
        "tag_name" = {% if tag_name | length %} "{{ tag_name }}" {% else %} null {% endif %}
    }
}

region_name = "{{ region_name }}"

account_id = "{{ account_id }}"

role_name = "{{ role_name }}"